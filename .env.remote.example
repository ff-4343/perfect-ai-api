PORT=3000
# استبدل بـ DATABASE_URL الخاص بقاعدة Render
# مثال: postgresql://user:password@hostname:5432/database?sslmode=require
DATABASE_URL=postgresql://USER:PASS@HOST:5432/perfect_ai?sslmode=require

# Optional: allow only these origins (comma separated). Leave empty to allow all.
# مثال:
# CORS_ORIGIN=https://your-frontend.com,https://admin.your-frontend.com
CORS_ORIGIN=