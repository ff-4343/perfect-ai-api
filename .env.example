PORT=3000
# الصق هنا connection string من قاعدة Render كما هو (غالباً يحتوي على SSL)
# مثال تقريبي (عدّل القيم):
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DBNAME?sslmode=require&schema=public"
# Optional: allow only these origins (comma separated). Leave empty to allow all.
# مثال:
# CORS_ORIGIN=https://your-frontend.com,https://admin.your-frontend.com
CORS_ORIGIN=
