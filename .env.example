PORT=3000
DATABASE_URL=postgresql://USER:PASS@HOST:5432/perfect_ai
# Optional: allow only these origins (comma separated). Leave empty to allow all.
# مثال:
# CORS_ORIGIN=https://your-frontend.com,https://admin.your-frontend.com
CORS_ORIGIN=
# Optional: rate limit max requests per minute (default: 120)
RATE_LIMIT_MAX=120
# Required: 64-character hex key for encrypting API keys (generate with: openssl rand -hex 32)
APP_SECRET=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
