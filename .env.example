# بورت الخادم | Server Port
PORT=3000

# رابط قاعدة البيانات | Database Connection URL
# مثال محلي | Local example: postgresql://username:password@localhost:5432/dbname
# مثال Render | Render example: postgresql://username:password@hostname:5432/dbname
DATABASE_URL=postgresql://username:password@localhost:5432/perfect_ai

# السماح للنطاقات المحددة فقط (مفصولة بفاصلة). اتركه فارغاً للسماح للجميع.
# Optional: allow only these origins (comma separated). Leave empty to allow all.
# مثال:
# CORS_ORIGIN=https://your-frontend.com,https://admin.your-frontend.com
CORS_ORIGIN=
